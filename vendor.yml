# Vendored files and directories are excluded from language
# statistics.
#
# Lines in this file are Regexps that are matched against the file
# pathname.
#
# Using this file to avoid our project being classified as "CSS" because
# of our inclusion of the font-awesome source in our repo

schedulecu/static/fontAwesome/* linguist-vendored